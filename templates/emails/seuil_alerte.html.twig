<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alerte de Seuil - Police Routière Guinée</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #dc2626;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 5px 5px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #dc2626;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .alert {
            padding: 15px;
            margin: 15px 0;
            border-radius: 5px;
        }
        .alert-danger {
            background-color: #fee2e2;
            border-left: 4px solid #ef4444;
        }
        .alert-warning {
            background-color: #fef3c7;
            border-left: 4px solid #f59e0b;
        }
        .info-box {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            margin: 15px 0;
            border: 1px solid #e5e7eb;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #f3f4f6;
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .label {
            font-weight: bold;
            color: #6b7280;
        }
        .value {
            color: #1f2937;
        }
        .threshold-value {
            font-size: 24px;
            font-weight: bold;
            color: #dc2626;
        }
        .threshold-label {
            font-size: 14px;
            color: #6b7280;
            margin-top: 5px;
        }
        .warning-icon {
            font-size: 48px;
            color: #dc2626;
            text-align: center;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🚨 Police Routière Guinée</h1>
        <p>ALERTE SYSTÈME</p>
    </div>

    <div class="content">
        <h2>🚨 Alerte de Seuil Dépassée</h2>
        <p>Bonjour,</p>
        
        <div class="warning-icon">
            ⚠️
        </div>
        
        <div class="alert alert-danger">
            <strong>🚨 Alerte critique - Seuil dépassé!</strong><br>
            {{ message }}
        </div>
        
        <div class="info-box">
            <h3>Détails de l'alerte</h3>
            <div class="info-row">
                <span class="label">Type d'alerte:</span>
                <span class="value">
                    {% if seuil_type == 'seuil_infractions' %}
                        📝 Seuil d'infractions
                    {% else %}
                        🛣️ Seuil de contrôles
                    {% endif %}
                </span>
            </div>
            <div class="info-row">
                <span class="label">Valeur actuelle:</span>
                <span class="value">
                    <span class="threshold-value">{{ seuil_valeur }}</span>
                </span>
            </div>
            <div class="info-row">
                <span class="label">Seuil configuré:</span>
                <span class="value">{{ seuil }}</span>
            </div>
            <div class="info-row">
                <span class="label">Date de détection:</span>
                <span class="value">{{ date }}</span>
            </div>
        </div>
        
        <div class="alert alert-warning">
            <strong>📊 Actions recommandées:</strong><br>
            <ul>
                {% if seuil_type == 'seuil_infractions' %}
                    <li>Vérifier les infractions récentes</li>
                    <li>Analyser les tendances des infractions</li>
                    <li>Considérer des actions préventives</li>
                {% else %}
                    <li>Vérifier les contrôles récents</li>
                    <li>Analyser la charge de travail</li>
                    <li>Considérer l'ajout de ressources</li>
                {% endif %}
            </ul>
        </div>
        
        <div style="margin-top: 20px;">
            <p>Connectez-vous au système pour analyser la situation et prendre les mesures nécessaires.</p>
            <a href="http://localhost:8000/dashboard" class="btn">
                Accéder au tableau de bord
            </a>
        </div>
    </div>

    <div class="footer">
        <p>&copy; 2024 Police Routière Guinée - Tous droits réservés</p>
        <p>Cet email a été généré automatiquement, veuillez ne pas répondre.</p>
    </div>
</body>
</html>
