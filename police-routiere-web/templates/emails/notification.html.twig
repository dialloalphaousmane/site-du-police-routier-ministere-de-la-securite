<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notification - Police Routière Guinée</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #1e3a8a;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 5px 5px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2563eb;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .alert {
            padding: 15px;
            margin: 15px 0;
            border-radius: 5px;
        }
        .alert-info {
            background-color: #e0f2fe;
            border-left: 4px solid #0ea5e9;
        }
        .alert-warning {
            background-color: #fef3c7;
            border-left: 4px solid #f59e0b;
        }
        .alert-danger {
            background-color: #fee2e2;
            border-left: 4px solid #ef4444;
        }
        .alert-success {
            background-color: #d1fae5;
            border-left: 4px solid #10b981;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🚔 Police Routière Guinée</h1>
        <p>Système de Gestion Routière</p>
    </div>

    <div class="content">
        <h2>Notification Système</h2>
        <p>Bonjour,</p>
        
        <p>Cette notification a été générée automatiquement par le système de gestion de la Police Routière Guinée.</p>
        
        {% if message %}
        <div class="alert alert-info">
            <strong>Message:</strong> {{ message }}
        </div>
        {% endif %}
        
        {% if seuil_type %}
        <div class="alert alert-warning">
            <strong>Alerte de seuil détectée:</strong><br>
            Type: {{ seuil_type }}<br>
            Valeur actuelle: {{ seuil_valeur }}<br>
            Seuil configuré: {{ seuil }}<br>
            {{ message }}
        </div>
        {% endif %}
        
        {% if success is defined %}
        <div class="alert alert-{{ success ? 'success' : 'danger' }}">
            <strong>Statut:</strong> {{ success ? 'Succès' : 'Échec' }}
        </div>
        {% endif %}
        
        {% if reset_link %}
        <div class="alert alert-info">
            <strong>Réinitialisation du mot de passe:</strong><br>
            Cliquez sur le lien ci-dessous pour réinitialiser votre mot de passe:<br>
            <a href="{{ reset_link }}" class="btn">Réinitialiser le mot de passe</a>
        </div>
        {% endif %}
        
        {% if login_link %}
        <div class="alert alert-success">
            <strong>Bienvenue dans le système!</strong><br>
            Vous pouvez maintenant vous connecter en utilisant le lien ci-dessous:<br>
            <a href="{{ login_link }}" class="btn">Se connecter</a>
        </div>
        {% endif %}
        
        <div style="margin-top: 30px;">
            <p><strong>Détails de la notification:</strong></p>
            <ul>
                <li>Date: {{ date }}</li>
                {% if user_email %}<li>Email: {{ user_email }}</li>{% endif %}
                {% if user_name %}<li>Nom: {{ user_name }}</li>{% endif %}
                {% if reset_token %}<li>Token: {{ reset_token }}</li>{% endif %}
            </ul>
        </div>
    </div>

    <div class="footer">
        <p>&copy; 2024 Police Routière Guinée - Tous droits réservés</p>
        <p>Cet email a été généré automatiquement, veuillez ne pas répondre.</p>
    </div>
</body>
</html>
