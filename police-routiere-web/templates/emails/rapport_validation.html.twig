<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Validation de Rapport - Police Routière Guinée</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #1e3a8a;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 5px 5px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2563eb;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .alert {
            padding: 15px;
            margin: 15px 0;
            border-radius: 5px;
        }
        .alert-success {
            background-color: #d1fae5;
            border-left: 4px solid #10b981;
        }
        .info-box {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            margin: 15px 0;
            border: 1px solid #e5e7eb;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #f3f4f6;
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .label {
            font-weight: bold;
            color: #6b7280;
        }
        .value {
            color: #1f2937;
        }
        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
        }
        .status-validated {
            background-color: #10b981;
            color: white;
        }
        .status-pending {
            background-color: #f59e0b;
            color: white;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🚔 Police Routière Guinée</h1>
        <p>Système de Gestion Routière</p>
    </div>

    <div class="content">
        <h2>Validation de Rapport</h2>
        <p>Bonjour,</p>
        
        <div class="alert alert-success">
            <strong>✅ Rapport validé avec succès!</strong><br>
            Le rapport "{{ rapport_titre }}" a été validé et est maintenant disponible dans le système.
        </div>
        
        <div class="info-box">
            <h3>Informations du rapport</h3>
            <div class="info-row">
                <span class="label">ID du rapport:</span>
                <span class="value">#{{ rapport_id }}</span>
            </div>
            <div class="info-row">
                <span class="label">Titre:</span>
                <span class="value">{{ rapport_titre }}</span>
            </div>
            <div class="info-row">
                <span class="label">Type:</span>
                <span class="value">{{ rapport_type }}</span>
            </div>
            <div class="info-row">
                <span class="label">Auteur:</span>
                <span class="value">{{ auteur }}</span>
            </div>
            <div class="info-row">
                <span class="label">Date de création:</span>
                <span class="value">{{ date_creation }}</span>
            </div>
            <div class="info-row">
                <span class="label">Validé par:</span>
                <span class="value">{{ validateur }}</span>
            </div>
            <div class="info-row">
                <span class="label">Date de validation:</span>
                <span class="value">{{ date_validation }}</span>
            </div>
            <div class="info-row">
                <span class="label">Statut:</span>
                <span class="value">
                    <span class="status-badge status-validated">VALIDÉ</span>
                </span>
            </div>
        </div>
        
        <div style="margin-top: 20px;">
            <p>Vous pouvez consulter ce rapport et d'autres documents dans le système de gestion.</p>
            <a href="http://localhost:8000/admin/report/{{ rapport_id }}" class="btn">
                Voir le rapport
            </a>
        </div>
    </div>

    <div class="footer">
        <p>&copy; 2024 Police Routière Guinée - Tous droits réservés</p>
        <p>Cet email a été généré automatiquement, veuillez ne pas répondre.</p>
    </div>
</body>
</html>
