<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nouveau Contrôle - Police Routière Guinée</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #1e3a8a;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 5px 5px;
        }
        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2563eb;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .alert {
            padding: 15px;
            margin: 15px 0;
            border-radius: 5px;
        }
        .alert-info {
            background-color: #e0f2fe;
            border-left: 4px solid #0ea5e9;
        }
        .info-box {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            margin: 15px 0;
            border: 1px solid #e5e7eb;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #f3f4f6;
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .label {
            font-weight: bold;
            color: #6b7280;
        }
        .value {
            color: #1f2937;
        }
        .stats {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin: 20px 0;
        }
        .stat-box {
            background-color: white;
            padding: 15px;
            border-radius: 5px;
            text-align: center;
            border: 1px solid #e5e7eb;
        }
        .stat-number {
            font-size: 24px;
            font-weight: bold;
            color: #1e3a8a;
        }
        .stat-label {
            font-size: 12px;
            color: #6b7280;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🚔 Police Routière Guinée</h1>
        <p>Système de Gestion Routière</p>
    </div>

    <div class="content">
        <h2>Nouveau Contrôle Enregistré</h2>
        <p>Bonjour,</p>
        
        <div class="alert alert-info">
            <strong>📋 Nouveau contrôle routier enregistré</strong><br>
            Un nouveau contrôle a été ajouté au système et nécessite votre attention.
        </div>
        
        <div class="info-box">
            <h3>Informations du contrôle</h3>
            <div class="info-row">
                <span class="label">ID du contrôle:</span>
                <span class="value">#{{ controle_id }}</span>
            </div>
            <div class="info-row">
                <span class="label">Type de contrôle:</span>
                <span class="value">{{ controle_type }}</span>
            </div>
            <div class="info-row">
                <span class="label">Lieu:</span>
                <span class="value">{{ controle_lieu }}</span>
            </div>
            <div class="info-row">
                <span class="label">Agent responsable:</span>
                <span class="value">{{ agent }}</span>
            </div>
            <div class="info-row">
                <span class="label">Date:</span>
                <span class="value">{{ date }}</span>
            </div>
        </div>
        
        <div class="stats">
            <div class="stat-box">
                <div class="stat-number">{{ vehicules_controles }}</div>
                <div class="stat-label">Véhicules contrôlés</div>
            </div>
            <div class="stat-box">
                <div class="stat-number">{{ infractions }}</div>
                <div class="stat-label">Infractions détectées</div>
            </div>
        </div>
        
        {% if infractions > 0 %}
        <div class="alert alert-warning">
            <strong>⚠️ Infractions détectées</strong><br>
            Ce contrôle a révélé {{ infractions }} infraction(s) qui nécessitent un traitement.
        </div>
        {% endif %}
        
        <div style="margin-top: 20px;">
            <p>Vous pouvez consulter les détails complets de ce contrôle dans le système de gestion.</p>
            <a href="http://localhost:8000/admin/controle/{{ controle_id }}" class="btn">
                Voir le contrôle
            </a>
        </div>
    </div>

    <div class="footer">
        <p>&copy; 2024 Police Routière Guinée - Tous droits réservés</p>
        <p>Cet email a été généré automatiquement, veuillez ne pas répondre.</p>
    </div>
</body>
</html>
